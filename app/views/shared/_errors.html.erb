<% if obj && obj.errors.any? %>
<div>
    <h4><%= pluralize(obj.errors.count, 'error') %> prohibited this room from being saved:</h4>
    <ul>
        <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %>